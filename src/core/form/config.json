{
    "name": "form",
    "role": "form_api",
    "tables": {
        "form_handlers": [
            "id int unsigned not null unique auto_increment primary key",
            "path_prefix varchar(100) not null unique",
            "handler_module varchar(200) not null"
        ],
        "form_tokens": [
            "id int unsigned not null unique auto_increment primary key",
            "url varchar(200) not null",
            "token varbinary(16) not null unique"
        ]
    }
}