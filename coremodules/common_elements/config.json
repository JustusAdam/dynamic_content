{
    "db_tables": [
        {
            "columns": [
                "id int unsigned not null auto_increment unique primary key",
                "user_first_name varchar(100)",
                "user_middle_name varchar(200)",
                "user_last_name varchar(100)",
                "access_group int unsigned not null",
                "date_created timestamp default current_timestamp",
                "date_changed timestamp default current_timestamp on update current_timestamp"
            ],
            "table_name": "cms_users"
        },
        {
            "columns": [
                "id int unsigned not null unique primary key",
                "user_id int unsigned not null unique",
                "user_password varchar(500) not null unique",
                "user_salt varchar(500) not null unique"
            ],
            "table_name": "cms_user_authentication"
        }
    ]
}