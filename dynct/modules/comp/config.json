{
    "name": "comp",
    "role": "theme_engine",
    "active_theme_path": "themes/default_theme",
    "active_theme": "default_theme",
    "default_theme": "default_theme",
    "admin_theme": "admin_theme",
    "tables": {
        "commons_custom_access": [
            "cid int unsigned not null unique references commons_config (cid) on delete cascade on update cascade",
            "rule tinyint comment '0 = no rule applied, 1 = visible by default & exclusion, 2 = hidden by default & inclusion'",
            "rule_exceptions text"
        ],
        "regions": [
            "rid int unsigned not null unique auto_increment primary key",
            "item_name varchar(200) not null references commons_config (element_name) on update cascade on delete cascade",
            "region varchar(200) not null",
            "weight smallint",
            "theme varchar(200) not null",
            "render_args varchar(64)",
            "description varchar(300)",
            "show_title boolean not null default true"
        ]
    }
}